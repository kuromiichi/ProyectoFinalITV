CREATE TABLE IF NOT EXISTS EstacionTable
(
    id        INTEGER   PRIMARY KEY AUTOINCREMENT,
    nombre    TEXT      NOT NULL,
    direccion TEXT      NOT NULL,
    correo    TEXT      NOT NULL,
    telefono  TEXT      NOT NULL
);

insertAll:
INSERT INTO EstacionTable (nombre, direccion, correo, telefono)
VALUES ('ITV DAM', 'Paseo de la Ermita 15', 'itvdam@itvdam.org', '+34822659855');

deleteAll:
DELETE FROM EstacionTable;

-- Obtener todo
selectAll:
SELECT * FROM EstacionTable;

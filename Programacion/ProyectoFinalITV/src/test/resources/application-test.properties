# Conexión que utilizaremos únicamente para borrar y crear la base de datos
db.url.connection.localhost=jdbc:mariadb://localhost:3306
# Configuración de tu perfil para la conexión con MariaDb
db.url.connection=jdbc:mariadb://localhost:3306/itvdamtest
db.name=itvdamtest
db.user=root
db.pass=1234
# Si está en 'true' la base de datos se cargará con los valores por defecto
db.default=true
